version: '3.8'

services:
  rippleguard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rippleguard-app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - HOST=0.0.0.0
      - CORS_ORIGINS=*
    restart: unless-stopped
